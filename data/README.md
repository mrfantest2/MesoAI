# Private data boundary

Do not put WhatsApp exports, audio, transcripts, embeddings, or generated voice samples in Git.
Use a local private root such as `C:\MesoAI\private`.
Only aggregate non-sensitive status data may be copied to `web/state.local.json`.
